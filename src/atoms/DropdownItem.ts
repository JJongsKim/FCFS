import { atom } from 'recoil';

export const CateDropDownAtom = atom({
  key: 'CateDropDownAtom',
  default: '카테고리',
});

export const NumDropDownAtom = atom({
  key: 'NumDropDownAtom',
  default: '인원',
});
